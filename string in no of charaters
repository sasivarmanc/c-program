#include<stdio.h>
#include<string.h>
int main()
{
    char v[100];
    int count=0;
    printf("Enter the letters: ");
    scanf("%s",v);
    for(int i=0;v[i]!='\0';i++)
    {
        count++;
    }
    printf("%d",count);
    return 0;
}
